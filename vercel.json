{
  "framework": "vite",
  "buildCommand": "cd artifacts/dementia-detection && pnpm build",
  "outputDirectory": "artifacts/dementia-detection/dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
